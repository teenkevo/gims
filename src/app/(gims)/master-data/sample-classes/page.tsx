import { SampleClasses } from "@/features/customer/services/components/sample-classes/sample-classes";

export default async function SampleClassesPage() {
  return (
    <div className="flex-col md:flex">
      <SampleClasses />
    </div>
  );
}
